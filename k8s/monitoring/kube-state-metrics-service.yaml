apiVersion: v1
kind: Service
metadata:
  name: kube-state-metrics
  namespace: service-launchpad-observability
  labels:
    app.kubernetes.io/name: kube-state-metrics
    app.kubernetes.io/component: cluster-metadata
    app.kubernetes.io/part-of: service-launchpad
spec:
  selector:
    app.kubernetes.io/name: kube-state-metrics
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
